% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/poolFilter.R
\name{poolFilter}
\alias{poolFilter}
\title{Filter Foreground and Background ID Sets by Gene Type}
\usage{
poolFilter(mapped, geneType = NULL)
}
\arguments{
\item{mapped}{A list returned by \code{mapIDs()}, containing at least:
\itemize{
\item \code{fg_ids}: data.frame with columns \code{entrez} and \code{mappedID} (foreground).
\item \code{bg_ids}: data.frame with columns \code{entrez} and \code{mappedID} (background).
\item \code{so_obj}: a \code{src_organism} object for transcript lookups.
\item \code{orgdb}:  the loaded OrgDb package object.
\item \code{transcript}: logical, whether IDs are transcripts.
}}

\item{geneType}{Optional character scalar; if not NULL, only genes of this
biotype (\code{GENETYPE} in the OrgDb) will be retained.  Valid values vary by
organism (e.g. “protein-coding”, “lncRNA”, etc.).}
}
\value{
The original \code{mapped} list, but with \code{fg_ids} and \code{bg_ids} replaced by
filtered versions (only rows matching \code{geneType}, if provided).
}
\description{
\code{poolFilter()} takes the mapped foreground and background ID data frames
(as produced by \code{mapIDs()}) and, if requested, filters both sets
to only include genes (or their transcripts) of a specified biotype
(e.g. “protein-coding”).
}
\examples{
\dontrun{
mapping  <- buildMappingObject("Homo sapiens")
mapped   <- mapIDs(my_ids, mapping)
filtered <- poolFilter(mapped, geneType = "protein-coding")
}

}
\keyword{internal}
