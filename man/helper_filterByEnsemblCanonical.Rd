% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper_filterByEnsemblCanonical.R
\name{helper_filterByEnsemblCanonical}
\alias{helper_filterByEnsemblCanonical}
\title{Filter a transcript‐coordinate table to Ensembl canonical transcripts}
\usage{
helper_filterByEnsemblCanonical(df, ensdb)
}
\arguments{
\item{df}{A data.frame or tibble containing at least a column
\code{tx_name}, which holds versioned Ensembl transcript IDs
(e.g. “ENST0000…​.1”).}

\item{ensdb}{An \code{EnsDb} object (from AnnotationHub or elsewhere).}
}
\value{
A data.frame containing only the rows of \code{df} whose transcript
(after stripping the “.\d+” suffix) is marked canonical in \code{ensdb}.
}
\description{
\code{helper_filterByEnsemblCanonical()} takes a data frame of transcript
coordinates (with a \code{tx_name} column), queries an \code{EnsDb} for
the set of canonical transcript IDs, and returns only those rows whose
(version‐stripped) \code{tx_name} matches a canonical transcript.
}
\details{
Internally, this helper:
\enumerate{
\item Uses \code{AnnotationDbi::select()} on \code{ensdb} to retrieve all
\code{TXID} ↔ \code{TXISCANONICAL} mappings.
\item Filters to those with \code{TXISCANONICAL == 1}.
\item Strips the trailing “.\d+” from \code{tx_name} in \code{df} to get base
transcript IDs.
\item Keeps only those rows whose base IDs appear in the canonical set.
}
}
\keyword{internal}
