% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper_loadEnsDbForOrganism.R
\name{helper_loadEnsDbForOrganism}
\alias{helper_loadEnsDbForOrganism}
\title{Find and load the appropriate EnsDb for a given organism + genome build}
\usage{
helper_loadEnsDbForOrganism(organism, genomeBuild)
}
\arguments{
\item{organism}{Character(1). Latin or common name of the organism
(e.g. “Homo sapiens” or “human”).}

\item{genomeBuild}{Character(1). UCSC‐style genome build (e.g. “hg38”).}
}
\value{
An \code{EnsDb} object loaded from AnnotationHub.
}
\description{
\code{helper_loadEnsDbForOrganism()} looks up the correct Ensembl
transcript database (EnsDb) in AnnotationHub for a given species
and UCSC‐style genome build (e.g. “hg38”), and returns the freshest
EnsDb object.
}
\details{
\enumerate{
\item Uses \code{GenomeInfoDb::mapGenomeBuilds()} to translate the UCSC
build into one or more Ensembl build identifiers (including patched
versions).
\item Queries \code{AnnotationHub()} for all “EnsDb” records for the
requested organism.
\item Filters those in‐memory to retain only the builds matching your
UCSC → Ensembl mapping.
\item Picks the record with the latest \code{rdatadateadded} metadata and
returns it.
}
}
\keyword{internal}
