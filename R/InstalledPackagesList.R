# install.packages("BiocManager")                     # if it’s not there yet
# BiocManager::install(version = BiocManager::version())
# # this makes sure you're on the right Bioconductor release for your R
#
# install.packages("devtools")

# install.packages(c("dplyr", "readr", "stringr", "tidyr"))


# BiocManager::install("nullranges")

# install.packages("ks")

BiocManager::install("GenomicFeatures")
